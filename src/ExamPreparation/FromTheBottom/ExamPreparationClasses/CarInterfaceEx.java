package ExamPreparation.FromTheBottom.ExamPreparationClasses;

import FirstSemester.OOP.GameObject;

import java.awt.*;

public class CarInterfaceEx implements GameObject {
    @Override
    public void move() {

    }

    @Override
    public boolean draw(Canvas canvas) {
        //canvas.drawCircle(); // wheel one
        //canvas.drawCircle(); // wheel two
        //same its only and example the code won't work
        // return true when done drawing
        return false;
    }
}
