package ExamPreparation.FromTheBottom.ExamPreparationClasses;

import FirstSemester.OOP.GameObject;

import java.awt.*;

public class Player implements GameObject {
    public void move() {

    }

    public boolean draw(Canvas canvas) {


        //canvas.drawCircle(); // head
        //canvas.drawLine(); // arm
        //canvas.drawLine(); // other arm
        //etc..
        //this is only an example the code doesn't work
        //but when done = return true
        return false;
    }
}
